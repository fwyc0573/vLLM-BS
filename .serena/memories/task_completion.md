# Task Completion (vLLM)

## Before finishing a change
- Run relevant tests (`pytest tests` or a narrower subset).
- Run lint/format via `pre-commit run --all-files` if style-sensitive files are touched.
- Update documentation or comments when behavior changes.